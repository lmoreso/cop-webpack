import _ from 'lodash';

/**
 * Take a string and return a css style declaration color
 * @param {string} color
 * @returns {string}
 */
const makeColorStyle = color => `color: ${color};`;

export default makeColorStyle;
